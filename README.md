# Puntenshawl! A sound relationship plaything for the purpose of punning.

## So you want to run it?

1. Clone the repo
2. `python v2data_parser.py -w "[word to get sound relationships on"`
3. Read the terminal output. Redirect it to a file if the matches overrun your terminal's buffer/scrollback.